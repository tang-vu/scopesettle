# ScopeSettle X launch posts

Use the project account `@scopesettle` for the official submission post. Attach
`docs/assets/scopesettle-demo.mp4` directly to post 1, publish posts 2–4 as replies, and pin post 1.
Then use `@tangvu_dev` to quote-post the thread.

## Official launch thread — `@scopesettle`

### Post 1 — attach the 90-second video

```text
Introducing ScopeSettle — verified work, automatic settlement.

Evidence-linked AI evaluation for ERC-8183 escrow on X Layer. Pin the deliverable, evaluate against an immutable rubric, and independently verify the verdict.

Built for AI Season by @XLayerOfficial. 🧵
```

### Post 2 — reply to post 1

```text
Payment rails can move money, but they cannot determine whether offchain work met an agreed scope.

ScopeSettle combines deterministic gates, cited AI evaluation, and a named trusted reviewer for ambiguous results.
```

### Post 3 — reply to post 2

```text
The result is inspectable end to end:

• immutable rubric and settlement policy
• pinned pull request and commits
• evidence-linked scoring
• public 10/10 integrity certificate
• open verifier SDK and CLI
• scoped API keys, signed webhooks, and audit trail
```

### Post 4 — reply to post 3

```text
Try ScopeSettle:
https://scopesettle.vercel.app

Inspect public Testnet job 3:
https://scopesettle.vercel.app/jobs/1952/3

Source and reproducible verification:
https://github.com/tang-vu/scopesettle

#XLayer #BuildX #AIAgents
```

## Founder quote-post — `@tangvu_dev`

Quote post 1 from the official thread:

```text
I built ScopeSettle around a missing primitive in agent commerce:

Payment rails can move money, but they cannot judge whether offchain work met the agreed scope.

The verdict is evidence-linked and independently reproducible from X Layer RPC. Demo below ↓
```

## Optional founder reply

Reply to the founder quote-post only if useful:

```text
ScopeSettle deliberately fails closed: ambiguous evidence goes to the named trusted reviewer instead of triggering an automatic payout.

The public demo shows a real valueless Testnet escrow refund, not a simulated transaction.
```

## Publishing checklist

- Post from `@scopesettle`, mention `@XLayerOfficial`, and keep that account active through the
  submission period.
- Attach the checked-in 90-second MP4 to post 1 instead of posting only a video link.
- Pin post 1 and use its URL in the official submission form.
- Quote-post from `@tangvu_dev` after the full project thread is published.
- Do not describe the evaluator as a trustless oracle.
- Do not claim Mainnet jobs, users, adoption, or trading volume.
