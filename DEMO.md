# 90-second judge demo

**0:00–0:12 — problem.** Open `/`. Say: “Agents can pay each other, but payment rails cannot tell
whether an offchain pull request met scope. ScopeSettle adds evidence, explanation, and settlement.”
Click **Explore a verified job**.

**0:12–0:32 — auditability.** Show the live completed Testnet job (replace the illustrative fixture
only after deployment). Point to the exact head SHA, separate deterministic checks, criterion-level
AI citations, weighted score, confidence, limitations, and report hash.

**0:32–0:48 — immutable agreement.** Open `/jobs/new`. Show weighted criteria summing to 100,
provider/budget/expiry, pass/confidence thresholds, challenge window, and the specification/rubric
hashes in the review step.

**0:48–1:08 — protocol.** Return to the job timeline. Explain create/fund/submit on the ERC-8183
lifecycle, the EIP-712 verdict proposal, challenge window, and permissionless finalization. Open one
real X Layer explorer transaction and the verified contract.

**1:08–1:24 — why the stack matters.** Say: “AI is essential for semantic rubric evaluation;
deterministic code validates identity, bounds, CI, evidence, and score math. X Layer anchors the
agreement and releases or refunds funds without web-app custody.”

**1:24–1:30 — close.** Say: “Verified work. Automatic settlement.” Show the beta disclosure.

Backup if wallet/RPC is slow: use the public completed Testnet URL and pre-opened explorer tabs.
Never substitute the illustrative fixture while calling it live.
